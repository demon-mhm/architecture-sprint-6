---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scalable-pod-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scalable-pod
  template:
    metadata:
      labels:
        app: scalable-pod
    spec:
      containers:
        - name: workload
          image: shestera/scaletestapp
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              memory: "10Mi"
            limits:
              memory: "20Mi"
